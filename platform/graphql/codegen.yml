overwrite: true
schema: "./graphql/generated/schema.graphql"
generates:
  ./lib/generated/graphql.ts:
    plugins:
      - typescript
    config:
      scalars:
        BigDecimal: string
        BigInt: string
